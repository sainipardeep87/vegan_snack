<%= form_for :spree_user,:url=>"/wizard_save" do |f| %>

  <%= render "#{@user.current_step}_step", :f => f %>
  <p><%= f.submit "Continue" %></p>
  <p><%= f.submit "Back", :name => "back_button" unless @user.first_step? %></p>
<% end %>
