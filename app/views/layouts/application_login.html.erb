<!DOCTYPE html>
<html>
    <head>
      <title>Vegan</title>

      <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0;" />
      <meta http-equiv="pragma" content="no-cache" />

      <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track' => true %>
      <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
      <%= javascript_include_tag "admin/spree_backend" if @spree_admin_js%>
      <%= favicon_link_tag '/assets/favicon/vegan.ico' %>

      <%= csrf_meta_tags %>
      <script type="text/javascript">
          if(navigator.userAgent.match(/iPhone/i)) {
              viewport = document.querySelector("meta[name=viewport]");
              viewport.setAttribute('content', 'width=600');
          }
          if(navigator.userAgent.match(/iPad/i)) {
              viewport = document.querySelector("meta[name=viewport]");
              viewport.setAttribute('content', 'width=1200');
          }
      </script>
    </head>

    <body>
        <noscript>
        <div class="js-disabled-header">
            <%= render 'overrides/Home/no_javascript_header' %>
        </div>
    </noscript>

      <div class="js-enabled-header">
          <%= render 'overrides/Home/header' %>
      </div>

  <%= yield %>
    
    </body>

</html>